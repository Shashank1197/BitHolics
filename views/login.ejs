<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Virtual Police Station</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        }
        .police-header {
            background: linear-gradient(to right, #1a237e, #0d47a1);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .police-badge {
            background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
            border: 2px solid #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }
        .emergency-btn {
            background: linear-gradient(135deg, #ff0000 0%, #cc0000 100%);
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
        .login-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        .feature-card {
            transition: transform 0.3s ease;
        }
        .feature-card:hover {
            transform: translateY(-5px);
        }
    </style>
</head>
<body class="min-h-screen">
    <!-- Header with Emergency Button -->
    <header class="police-header text-white">
        <div class="container mx-auto px-4 py-3">
            <div class="flex justify-between items-center">
                <div class="flex items-center space-x-4">
                    <div class="police-badge w-12 h-12 rounded-full flex items-center justify-center">
                        <span class="text-2xl">⚡</span>
                    </div>
                    <div>
                        <h1 class="text-2xl font-bold">Virtual Police Station</h1>
                        <p class="text-sm text-gray-200">Government of India</p>
                    </div>
                </div>
                <a href="/emergency" class="emergency-btn px-6 py-2 rounded-full font-bold flex items-center space-x-2">
                    <span>🚨</span>
                    <span>Emergency</span>
                </a>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-8">
        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <!-- Login Form -->
            <div class="lg:col-span-2">
                <div class="login-card rounded-xl shadow-xl p-8">
                    <h2 class="text-3xl font-bold text-gray-800 mb-2">Citizen Login</h2>
                    <p class="text-gray-600 mb-6">Access your police services securely</p>
                    
                    <form id="loginForm" class="space-y-6">
                        <div>
                            <label for="aadhaar" class="block text-sm font-medium text-gray-700">Aadhaar Number</label>
                            <input type="text" id="aadhaar" name="aadhaar" 
                                   class="mt-1 block w-full rounded-lg border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500"
                                   pattern="\d{12}" maxlength="12" required
                                   placeholder="Enter 12-digit Aadhaar number">
                        </div>

                        <div>
                            <label for="phone" class="block text-sm font-medium text-gray-700">Mobile Number</label>
                            <input type="text" id="phone" name="phone" 
                                   class="mt-1 block w-full rounded-lg border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500"
                                   pattern="\d{10}" maxlength="10" required
                                   placeholder="Enter 10-digit mobile number">
                        </div>

                        <div id="otpSection" class="hidden">
                            <label for="otp" class="block text-sm font-medium text-gray-700">OTP Verification</label>
                            <div class="flex space-x-4">
                                <input type="text" id="otp" name="otp"
                                       class="mt-1 block w-full rounded-lg border-gray-300 shadow-sm focus:border-blue-500 focus:ring-blue-500"
                                       pattern="\d{6}" maxlength="6" required
                                       placeholder="Enter 6-digit OTP">
                                <div id="timer" class="mt-1 text-sm text-gray-500 hidden flex items-center">
                                    <span class="bg-gray-100 px-3 py-1 rounded-lg">
                                        <span id="minutes">05</span>:<span id="seconds">00</span>
                                    </span>
                                </div>
                            </div>
                        </div>

                        <div class="flex justify-between items-center">
                            <button type="button" id="requestOTP" 
                                    class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition flex items-center space-x-2">
                                <span>🔐</span>
                                <span>Request OTP</span>
                            </button>
                            <button type="submit" id="submitOTP" 
                                    class="bg-green-600 text-white px-6 py-2 rounded-lg hover:bg-green-700 transition flex items-center space-x-2 hidden">
                                <span>✓</span>
                                <span>Verify OTP</span>
                            </button>
                        </div>

                        <div id="error" class="text-red-500 text-sm hidden"></div>
                    </form>

                    <div class="mt-6 text-center">
                        <p class="text-sm text-gray-600">
                            Don't want to login? <a href="/fir/new" class="text-blue-600 hover:underline">File Anonymous FIR</a>
                        </p>
                    </div>
                </div>
            </div>

            <!-- Quick Links -->
            <div class="space-y-6">
                <div class="bg-white rounded-xl shadow-xl p-6">
                    <h3 class="text-xl font-bold text-gray-800 mb-4">Quick Services</h3>
                    <div class="space-y-4">
                        <a href="/fir/new" class="feature-card block p-4 bg-gray-50 rounded-lg hover:bg-gray-100">
                            <div class="flex items-center space-x-3">
                                <span class="text-2xl">📝</span>
                                <span class="font-medium">File FIR</span>
                            </div>
                        </a>
                        <a href="/track" class="feature-card block p-4 bg-gray-50 rounded-lg hover:bg-gray-100">
                            <div class="flex items-center space-x-3">
                                <span class="text-2xl">🔍</span>
                                <span class="font-medium">Track FIR Status</span>
                            </div>
                        </a>
                        <a href="/complaints" class="feature-card block p-4 bg-gray-50 rounded-lg hover:bg-gray-100">
                            <div class="flex items-center space-x-3">
                                <span class="text-2xl">📢</span>
                                <span class="font-medium">Register Complaint</span>
                            </div>
                        </a>
                        <a href="/verification" class="feature-card block p-4 bg-gray-50 rounded-lg hover:bg-gray-100">
                            <div class="flex items-center space-x-3">
                                <span class="text-2xl">✅</span>
                                <span class="font-medium">Document Verification</span>
                            </div>
                        </a>
                    </div>
                </div>

                <div class="bg-white rounded-xl shadow-xl p-6">
                    <h3 class="text-xl font-bold text-gray-800 mb-4">Important Links</h3>
                    <div class="space-y-4">
                        <a href="/helpline" class="block text-blue-600 hover:underline">Police Helpline Numbers</a>
                        <a href="/missing" class="block text-blue-600 hover:underline">Missing Person Reports</a>
                        <a href="/wanted" class="block text-blue-600 hover:underline">Wanted Criminals</a>
                        <a href="/cyber" class="block text-blue-600 hover:underline">Cyber Crime Reporting</a>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white mt-12">
        <div class="container mx-auto px-4 py-8">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div>
                    <h4 class="text-lg font-bold mb-4">Contact Us</h4>
                    <p class="text-gray-300">24x7 Helpline: 100</p>
                    <p class="text-gray-300">Email: support@virtualpolice.gov.in</p>
                </div>
                <div>
                    <h4 class="text-lg font-bold mb-4">Quick Links</h4>
                    <ul class="space-y-2">
                        <li><a href="/about" class="text-gray-300 hover:text-white">About Us</a></li>
                        <li><a href="/privacy" class="text-gray-300 hover:text-white">Privacy Policy</a></li>
                        <li><a href="/terms" class="text-gray-300 hover:text-white">Terms of Service</a></li>
                    </ul>
                </div>
                <div>
                    <h4 class="text-lg font-bold mb-4">Follow Us</h4>
                    <div class="flex space-x-4">
                        <a href="#" class="text-gray-300 hover:text-white">Twitter</a>
                        <a href="#" class="text-gray-300 hover:text-white">Facebook</a>
                        <a href="#" class="text-gray-300 hover:text-white">Instagram</a>
                    </div>
                </div>
            </div>
            <div class="mt-8 pt-8 border-t border-gray-700 text-center text-gray-400">
                <p>© 2024 Virtual Police Station. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script>
        let timerInterval;
        let timeLeft = 300; // 5 minutes in seconds

        function startTimer() {
            const timerDisplay = document.getElementById('timer');
            const minutesDisplay = document.getElementById('minutes');
            const secondsDisplay = document.getElementById('seconds');
            
            timerDisplay.classList.remove('hidden');
            
            timerInterval = setInterval(() => {
                const minutes = Math.floor(timeLeft / 60);
                const seconds = timeLeft % 60;
                
                minutesDisplay.textContent = minutes.toString().padStart(2, '0');
                secondsDisplay.textContent = seconds.toString().padStart(2, '0');
                
                if (timeLeft <= 0) {
                    clearInterval(timerInterval);
                    document.getElementById('otpSection').classList.add('hidden');
                    document.getElementById('submitOTP').classList.add('hidden');
                    document.getElementById('requestOTP').classList.remove('hidden');
                    document.getElementById('error').textContent = 'OTP expired. Please request a new one.';
                    document.getElementById('error').classList.remove('hidden');
                }
                
                timeLeft--;
            }, 1000);
        }

        document.getElementById('requestOTP').addEventListener('click', async function() {
            const aadhaar = document.getElementById('aadhaar').value;
            const phone = document.getElementById('phone').value;
            const errorDiv = document.getElementById('error');
            
            if (aadhaar.length !== 12) {
                errorDiv.textContent = 'Please enter a valid 12-digit Aadhaar number';
                errorDiv.classList.remove('hidden');
                return;
            }
            
            if (phone.length !== 10) {
                errorDiv.textContent = 'Please enter a valid 10-digit phone number';
                errorDiv.classList.remove('hidden');
                return;
            }
            
            try {
                const response = await fetch('/generate-otp', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ aadhaar, phone }),
                });
                
                const data = await response.json();
                
                if (data.success) {
                    document.getElementById('otpSection').classList.remove('hidden');
                    document.getElementById('submitOTP').classList.remove('hidden');
                    this.classList.add('hidden');
                    errorDiv.classList.add('hidden');
                    timeLeft = data.timer;
                    startTimer();
                } else {
                    errorDiv.textContent = data.error;
                    errorDiv.classList.remove('hidden');
                }
            } catch (error) {
                errorDiv.textContent = 'Error generating OTP. Please try again.';
                errorDiv.classList.remove('hidden');
            }
        });

        document.getElementById('loginForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            const otp = document.getElementById('otp').value;
            const errorDiv = document.getElementById('error');
            
            try {
                const response = await fetch('/verify-otp', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({ otp }),
                });
                
                const data = await response.json();
                
                if (data.success) {
                    window.location.href = data.redirect;
                } else {
                    errorDiv.textContent = data.error;
                    errorDiv.classList.remove('hidden');
                }
            } catch (error) {
                errorDiv.textContent = 'Error verifying OTP. Please try again.';
                errorDiv.classList.remove('hidden');
            }
        });
    </script>
</body>
</html> 