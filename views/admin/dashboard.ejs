<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Virtual Police Station</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <nav class="bg-[#001f3f] text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <div class="flex items-center space-x-2">
                <div class="police-badge w-10 h-10 rounded-full flex items-center justify-center">
                    <span class="text-[#FFD700] text-xl">⚡</span>
                </div>
                <h1 class="text-xl font-bold">Police Admin Dashboard</h1>
            </div>
            <div class="space-x-4">
                <span class="text-[#FFD700]">Officer: SI Sharma</span>
                <a href="/admin/logout" class="hover:text-[#FFD700]">Logout</a>
            </div>
        </div>
    </nav>

    <main class="container mx-auto p-4">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-6">
            <div class="bg-white rounded-lg shadow p-4">
                <h3 class="text-gray-500">Total FIRs</h3>
                <p class="text-2xl font-bold text-[#001f3f]">156</p>
            </div>
            <div class="bg-white rounded-lg shadow p-4">
                <h3 class="text-gray-500">Pending Review</h3>
                <p class="text-2xl font-bold text-yellow-600">23</p>
            </div>
            <div class="bg-white rounded-lg shadow p-4">
                <h3 class="text-gray-500">Under Investigation</h3>
                <p class="text-2xl font-bold text-blue-600">45</p>
            </div>
            <div class="bg-white rounded-lg shadow p-4">
                <h3 class="text-gray-500">Closed Cases</h3>
                <p class="text-2xl font-bold text-green-600">88</p>
            </div>
        </div>

        <div class="bg-white rounded-lg shadow-lg p-6">
            <div class="flex justify-between items-center mb-6">
                <h2 class="text-2xl font-bold text-[#001f3f]">FIR Management</h2>
                <div class="flex space-x-4">
                    <select id="stationFilter" class="rounded-md border-gray-300 shadow-sm focus:border-[#001f3f] focus:ring-[#001f3f]">
                        <option value="all">All Stations</option>
                        <option value="main">Main Police Station</option>
                        <option value="north">North Police Station</option>
                    </select>
                    <input type="text" id="searchFir" placeholder="Search FIR..." 
                           class="rounded-md border-gray-300 shadow-sm focus:border-[#001f3f] focus:ring-[#001f3f]">
                </div>
            </div>

            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">FIR No.</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Complainant</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Type</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Assigned To</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        <!-- Sample FIR Row -->
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-[#001f3f]">FIR-2024-001</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">John Doe</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">Theft</td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <select class="rounded-md border-gray-300 shadow-sm focus:border-[#001f3f] focus:ring-[#001f3f] text-sm">
                                    <option value="Filed">Filed</option>
                                    <option value="Reviewing" selected>Reviewing</option>
                                    <option value="Investigating">Investigating</option>
                                    <option value="Closed">Closed</option>
                                </select>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">SI Sharma</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                <button class="text-[#001f3f] hover:underline mr-2">View</button>
                                <button class="text-[#001f3f] hover:underline">Add Note</button>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </main>

    <script>
        // In production, this would handle status updates and other admin actions
        document.querySelectorAll('select').forEach(select => {
            select.addEventListener('change', function() {
                const firId = this.closest('tr').querySelector('td:first-child').textContent;
                const newStatus = this.value;
                // Update FIR status in database
            });
        });
    </script>
</body>
</html> 