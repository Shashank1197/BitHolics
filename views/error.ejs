<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Virtual Police Station</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <nav class="bg-[#001f3f] text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <div class="flex items-center space-x-2">
                <div class="police-badge w-10 h-10 rounded-full flex items-center justify-center">
                    <span class="text-[#FFD700] text-xl">⚡</span>
                </div>
                <h1 class="text-xl font-bold">Virtual Police Station</h1>
            </div>
        </div>
    </nav>

    <main class="container mx-auto p-4">
        <div class="max-w-3xl mx-auto bg-white rounded-lg shadow-lg p-6">
            <div class="border-b-2 border-[#001f3f] pb-4 mb-6">
                <h2 class="text-2xl font-bold text-[#001f3f]">Error</h2>
            </div>

            <div class="space-y-4">
                <div class="bg-red-100 text-red-700 p-4 rounded-lg">
                    <h3 class="font-bold">Error Message:</h3>
                    <p><%= message %></p>
                </div>

                <% if (error && Object.keys(error).length > 0) { %>
                    <div class="bg-gray-100 p-4 rounded-lg">
                        <h3 class="font-bold">Technical Details:</h3>
                        <pre class="text-sm overflow-x-auto"><%= JSON.stringify(error, null, 2) %></pre>
                    </div>
                <% } %>

                <div class="flex justify-end">
                    <a href="/" class="bg-[#001f3f] text-white px-4 py-2 rounded hover:bg-blue-800 transition">
                        Return to Home
                    </a>
                </div>
            </div>
        </div>
    </main>
</body>
</html> 