<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        }
        .police-header {
            background: linear-gradient(to right, #1a237e, #0d47a1);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .police-badge {
            background: linear-gradient(135deg, #FFD700 0%, #FFA500 100%);
            border: 2px solid #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
        }
        .emergency-btn {
            background: linear-gradient(135deg, #ff0000 0%, #cc0000 100%);
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }
        .card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }
        .feature-card {
            transition: transform 0.3s ease;
        }
        .feature-card:hover {
            transform: translateY(-5px);
        }
    </style>
</head>
<body class="min-h-screen">
    <!-- Header -->
    <header class="police-header text-white">
        <div class="container mx-auto px-4 py-3">
            <div class="flex justify-between items-center">
                <div class="flex items-center space-x-4">
                    <div class="police-badge w-12 h-12 rounded-full flex items-center justify-center">
                        <span class="text-2xl">⚡</span>
                    </div>
                    <div>
                        <h1 class="text-2xl font-bold">Virtual Police Station</h1>
                        <p class="text-sm text-gray-200">Government of India</p>
                    </div>
                </div>
                <div class="flex items-center space-x-4">
                    <a href="/emergency" class="emergency-btn px-6 py-2 rounded-full font-bold flex items-center space-x-2">
                        <span>🚨</span>
                        <span>Emergency</span>
                    </a>
                    <a href="/logout" class="bg-gray-700 text-white px-4 py-2 rounded-lg hover:bg-gray-800 transition">
                        Logout
                    </a>
                </div>
            </div>
        </div>
    </header>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-8">
        <div class="grid grid-cols-1 lg:grid-cols-4 gap-8">
            <!-- User Profile Card -->
            <div class="lg:col-span-1">
                <div class="card rounded-xl shadow-xl p-6">
                    <div class="text-center mb-6">
                        <div class="w-24 h-24 bg-gray-200 rounded-full mx-auto mb-4 flex items-center justify-center">
                            <span class="text-4xl">👤</span>
                        </div>
                        <h2 class="text-xl font-bold text-gray-800"><%= user.name %></h2>
                        <p class="text-gray-600">Aadhaar: <%= user.aadhaar %></p>
                        <p class="text-gray-600">Phone: <%= user.phone %></p>
                    </div>
                    <div class="space-y-4">
                        <a href="/profile" class="block text-center bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
                            Edit Profile
                        </a>
                        <a href="/change-password" class="block text-center bg-gray-600 text-white px-4 py-2 rounded-lg hover:bg-gray-700 transition">
                            Change Password
                        </a>
                    </div>
                </div>
            </div>

            <!-- Quick Actions -->
            <div class="lg:col-span-3">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <!-- Recent FIRs -->
                    <div class="card rounded-xl shadow-xl p-6">
                        <h3 class="text-xl font-bold text-gray-800 mb-4">Recent FIRs</h3>
                        <% if (firs && firs.length > 0) { %>
                            <div class="space-y-4">
                                <% firs.forEach(fir => { %>
                                    <div class="bg-gray-50 p-4 rounded-lg">
                                        <div class="flex justify-between items-start">
                                            <div>
                                                <p class="font-medium text-gray-800">FIR #<%= fir._id.toString().slice(-6) %></p>
                                                <p class="text-sm text-gray-600"><%= fir.firDetails.crimeType %></p>
                                            </div>
                                            <span class="px-2 py-1 text-xs rounded-full 
                                                <%= fir.status === 'pending' ? 'bg-yellow-100 text-yellow-800' : 
                                                   fir.status === 'approved' ? 'bg-green-100 text-green-800' : 
                                                   'bg-red-100 text-red-800' %>">
                                                <%= fir.status %>
                                            </span>
                                        </div>
                                        <a href="/fir/<%= fir._id %>" class="text-blue-600 text-sm hover:underline mt-2 block">
                                            View Details
                                        </a>
                                    </div>
                                <% }); %>
                            </div>
                        <% } else { %>
                            <p class="text-gray-600">No FIRs filed yet</p>
                        <% } %>
                        <a href="/fir/new" class="mt-4 block text-center bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
                            File New FIR
                        </a>
                    </div>

                    <!-- Upcoming Appointments -->
                    <div class="card rounded-xl shadow-xl p-6">
                        <h3 class="text-xl font-bold text-gray-800 mb-4">Upcoming Appointments</h3>
                        <% if (appointments && appointments.length > 0) { %>
                            <div class="space-y-4">
                                <% appointments.forEach(appointment => { %>
                                    <div class="bg-gray-50 p-4 rounded-lg">
                                        <div class="flex justify-between items-start">
                                            <div>
                                                <p class="font-medium text-gray-800"><%= appointment.purpose %></p>
                                                <p class="text-sm text-gray-600">
                                                    <%= new Date(appointment.date).toLocaleDateString() %>
                                                    <%= appointment.time %>
                                                </p>
                                            </div>
                                            <span class="px-2 py-1 text-xs rounded-full 
                                                <%= appointment.status === 'pending' ? 'bg-yellow-100 text-yellow-800' : 
                                                   appointment.status === 'confirmed' ? 'bg-green-100 text-green-800' : 
                                                   'bg-red-100 text-red-800' %>">
                                                <%= appointment.status %>
                                            </span>
                                        </div>
                                        <a href="/appointments/<%= appointment._id %>" class="text-blue-600 text-sm hover:underline mt-2 block">
                                            View Details
                                        </a>
                                    </div>
                                <% }); %>
                            </div>
                        <% } else { %>
                            <p class="text-gray-600">No upcoming appointments</p>
                        <% } %>
                        <a href="/appointments/new" class="mt-4 block text-center bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700 transition">
                            Schedule Appointment
                        </a>
                    </div>

                    <!-- Quick Services -->
                    <div class="card rounded-xl shadow-xl p-6 md:col-span-2">
                        <h3 class="text-xl font-bold text-gray-800 mb-4">Quick Services</h3>
                        <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
                            <a href="/complaints" class="feature-card block p-4 bg-gray-50 rounded-lg hover:bg-gray-100 text-center">
                                <span class="text-3xl block mb-2">📢</span>
                                <span class="font-medium">Register Complaint</span>
                            </a>
                            <a href="/verification" class="feature-card block p-4 bg-gray-50 rounded-lg hover:bg-gray-100 text-center">
                                <span class="text-3xl block mb-2">✅</span>
                                <span class="font-medium">Document Verification</span>
                            </a>
                            <a href="/missing" class="feature-card block p-4 bg-gray-50 rounded-lg hover:bg-gray-100 text-center">
                                <span class="text-3xl block mb-2">🔍</span>
                                <span class="font-medium">Missing Person</span>
                            </a>
                            <a href="/cyber" class="feature-card block p-4 bg-gray-50 rounded-lg hover:bg-gray-100 text-center">
                                <span class="text-3xl block mb-2">💻</span>
                                <span class="font-medium">Cyber Crime</span>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white mt-12">
        <div class="container mx-auto px-4 py-8">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div>
                    <h4 class="text-lg font-bold mb-4">Contact Us</h4>
                    <p class="text-gray-300">24x7 Helpline: 100</p>
                    <p class="text-gray-300">Email: support@virtualpolice.gov.in</p>
                </div>
                <div>
                    <h4 class="text-lg font-bold mb-4">Quick Links</h4>
                    <ul class="space-y-2">
                        <li><a href="/about" class="text-gray-300 hover:text-white">About Us</a></li>
                        <li><a href="/privacy" class="text-gray-300 hover:text-white">Privacy Policy</a></li>
                        <li><a href="/terms" class="text-gray-300 hover:text-white">Terms of Service</a></li>
                    </ul>
                </div>
                <div>
                    <h4 class="text-lg font-bold mb-4">Follow Us</h4>
                    <div class="flex space-x-4">
                        <a href="#" class="text-gray-300 hover:text-white">Twitter</a>
                        <a href="#" class="text-gray-300 hover:text-white">Facebook</a>
                        <a href="#" class="text-gray-300 hover:text-white">Instagram</a>
                    </div>
                </div>
            </div>
            <div class="mt-8 pt-8 border-t border-gray-700 text-center text-gray-400">
                <p>© 2024 Virtual Police Station. All rights reserved.</p>
            </div>
        </div>
    </footer>
</body>
</html> 